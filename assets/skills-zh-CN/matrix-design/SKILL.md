---
name: matrix-design
description: 在实现前设计并冻结活动 Matrix 变更。
---

# Matrix Design

阅读 proposal，写入 `design.md`（`## Decisions`、`## Boundaries`、`## Test seams`、`## Risks`）与 `plan.md`（`## Steps`、`## Validation`、`## Stop conditions`）。重大范围或架构决策须先由用户确认。

```powershell
matrix workflow guard design
matrix workflow transition build
```
